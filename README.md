
# 🎓 NewTech Institute Zhob - Public Portal

This is the official public-facing website and internal management portal for **NewTech Institute Zhob**.

## 🚀 Quick Deployment

This project is optimized for **Zero-Config Deployment**.

### Vercel / Netlify
1.  Connect this repository to Vercel or Netlify.
2.  Add the `API_KEY` environment variable in the dashboard settings.
3.  The project will automatically deploy at your custom URL.

### Local Development
If you wish to run this locally for testing:
1.  Clone the repository.
2.  Run `npx serve .` in the root directory.
3.  Open `http://localhost:3000`.

## ⚙️ Configuration
- **Branding**: Logos and institute details can be updated via the **Admin -> Settings** tab within the app.
- **Persistence**: All data is currently stored in `localStorage` for high performance and offline capability. For multi-device sync, consider connecting to a database (e.g., Supabase or Firebase) in the future.
- **AI Features**: The AI Academic Assistant requires a valid Google Gemini API Key injected via environment variables.

## 📍 Campus HQ
Main Bazar Road, Near Post Office, Zhob, Balochistan.
